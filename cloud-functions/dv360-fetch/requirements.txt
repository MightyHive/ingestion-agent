# Cloud Function runtime dependencies for dv360-fetch.
#
# Pinned to compatible minor ranges (NOT exact pins) so:
#   * Security backports land automatically on redeploy.
#   * Cold-start time stays low (no resolver thrashing).
#
# Updates require redeploy + smoke test against a real DV360 query.

# HTTP entrypoint shim (Gen2 runtime supplies this, but listing it
# explicitly keeps the local functions-framework smoke deterministic).
functions-framework==3.*

# Secret Manager client — reads client_<tenant>_dv360_{query_id,service_account_json}
google-cloud-secret-manager>=2.20,<3

# BigQuery client — used to create table on first write + append rows
google-cloud-bigquery>=3.20,<4

# Service-account auth for the DV360 token mint inside dv360_reports
google-auth>=2.30

# Vendored connector dependencies (also listed in
# connectors-library/dv360/reports/requirements.txt; replicated here so
# deploy.sh produces a self-contained staging dir).
requests>=2.31.0
