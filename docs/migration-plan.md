<!--
Tombstone — original document was the phase-by-phase plan to migrate
from the multi-agent LLM ingestion graph to the deterministic pipeline.

Phases 0 through 5 (MVP) are all done. The plan is no longer useful as
forward-looking guidance; the historical record lives in git history
(tag `legacy-mds-agents`, branch `new-mds-deterministic`).

Remove with: rm docs/migration-plan.md
-->

# DELETED — refactor migration plan (Phases 0-5 done)

The multi-agent → deterministic migration is complete. For the rationale,
see `docs/adr/001-multi-agent-to-deterministic-pipeline.md`. For the
current architecture, see `docs/architecture.md`.

Safe to delete: `rm docs/migration-plan.md`.
