# Python 3.10+ recommended (typing, LangGraph).
#
# Phase 4 trimmed this down to the deterministic ingestion stack only.
# The legacy multi-agent graph (PydanticAI agents, langchain RAG, SQLite
# checkpointer, ChromaDB, Google generative AI) was deleted together with
# the agent source code — see git tag `legacy-mds-agents` for the prior
# requirements.txt.

# --- Web framework + ASGI server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# --- Deterministic ingestion graph (src/ingestion/graph.py)
langgraph>=0.2.0

# --- Pydantic v2 (request models, NodeLOL envelope, IngestionState)
pydantic>=2.7

# --- Manifest validation (Draft 2020-12 support landed in jsonschema >=4.18)
jsonschema>=4.20

# --- Tests
pytest>=8.0

# --- FastAPI TestClient requires httpx at runtime (used by test_api_run.py)
# Also used by HTTPBackend (Phase 5) to POST to Cloud Function URLs.
httpx>=0.27

# --- HTTPBackend id_token auth (Phase 5)
# fetch_id_token resolves ADC and exchanges for an OIDC token whose
# audience is the CF URL. Skipped when MDS_CF_BASE_URL points at
# localhost (functions-framework emulator / smoke tests).
google-auth>=2.30

# --- Env file loading
python-dotenv>=1.0

# --- Credentials module (Phase 5)
# SQLAlchemy for metadata persistence (connections table).
sqlalchemy>=2.0
# GCP Secret Manager for token storage in production.
# Set MDS_SECRETS_BACKEND=gcp to use it; defaults to local file backend.
# google-cloud-secretmanager>=2.20
